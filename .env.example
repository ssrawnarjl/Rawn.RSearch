# Search Provider: serper | ddg
SEARCH_PROVIDER=serper
# when SEARCH_PROVIDER=serper:
SERPER_API_KEY=put-your-serper-api-key-here

# Server
PORT=5173
HOST=0.0.0.0
# Allowed CORS origins (comma-separated). Leave empty to allow all (dev only).
CORS_ORIGINS=